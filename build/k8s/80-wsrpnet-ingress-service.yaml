apiVersion: v1
kind: Service
metadata:
  name: wsprnet-ingress-service
  namespace: wsprnet
spec:
  type: NodePort
  selector:
    app: wsprnet-ingress
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30001
